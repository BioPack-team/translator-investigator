## <bundle-name>

<A standing instruction the agent should follow whenever this bundle is enabled: when to reach for
it and how the dev works with it. Keep it short. Example (a tool): "Prefer `tt` for repro /
regression queries; run it non-interactively with explicit flags; symlink query files back into the
investigation dir.">
