{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          { "type": "command", "command": "replace-me — e.g. a formatter or logger" }
        ]
      }
    ]
  }
}
